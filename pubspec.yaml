name: agrofield
description: "AgroTech MVP - Captura offline de multimedia agricola"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.3
  path: ^1.9.0
  dio: ^5.4.0
  provider: ^6.1.2
  connectivity_plus: ^6.0.5
  image_picker: ^1.0.7
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  flutter_secure_storage: ^9.0.0
  uuid: ^4.3.3
  http: ^1.2.1
  cached_network_image: ^3.3.1
  mime: ^1.0.5
  video_player: ^2.8.3
  audioplayers: ^6.0.0
  record: ^4.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
